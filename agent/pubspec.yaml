name: gisila_agent
description: Privileged host-side deployment agent for the Gisila Panel.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.3.4

dependencies:
  args: ^2.5.0
  path: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
