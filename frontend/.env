# Development — API on localhost:8000, panel UI served by Vite dev server.
# In production (embedded in Dart), these vars are not set and the browser
# uses relative URLs against the same origin.
VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000
